## Running a Single Lab

To run a single lab:

1. Open the `file.f` file.
2. Replace the design and testbench file names with the ones corresponding to the lab you want to run.
3. Execute the following command:

```bash
xrun -f file.f
```

This will compile and run the specified design and testbench.
